{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.bailinghub.com/schemas/config/credential.schema.json",
  "title": "Bailing Credential Config",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "kind", "base_url", "api_key", "enabled"],
  "properties": {
    "name": {
      "$ref": "common.schema.json#/$defs/resourceName",
      "title": "名称",
      "description": "凭证稳定标识，路由和知识库按该名称引用。建好后不建议改名。"
    },
    "kind": {
      "type": "string",
      "enum": ["chat", "embedding", "both"],
      "title": "用途",
      "description": "这把密钥可用于对话类模型、向量化模型，或两者都可用。"
    },
    "base_url": {
      "type": "string",
      "minLength": 1,
      "title": "Base URL",
      "description": "模型服务的 OpenAI 兼容接口前缀。"
    },
    "api_key": {
      "type": "string",
      "title": "API Key",
      "description": "中枢出站调用模型服务时使用的密钥；编辑时留空表示保留原值。"
    },
    "default_model": {
      "type": "string",
      "minLength": 1,
      "title": "默认模型",
      "description": "路由或知识库未显式指定模型时使用的默认模型。"
    },
    "enabled": {
      "type": "boolean",
      "title": "启用",
      "description": "关闭后该凭证不再可被运行期使用。"
    },
    "description": {
      "type": "string",
      "title": "说明",
      "description": "给后台管理员看的补充备注。"
    }
  }
}
